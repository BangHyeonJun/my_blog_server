type Comment {
    publish_date: String!
    name: String!
    content: String
}

type Post {
    title: String!
    publish_date: String!
    content: String!
    comments: [Comment]
}

type Query {
    post: [Post]
}

type Mutation {
    insertPost(title: String!, content: String!): Boolean!
}
